# Server configuration
PORT=3000
HOST=0.0.0.0

# Database
MONGO_URI=mongodb://127.0.0.1:27017/Chess

# Secrets
SECRET_KEY=change_me

# Redis (optional; defaults to local if unset)
REDIS_URL=redis://127.0.0.1:6379

# CORS configuration
# Set to true to allow all origins (not recommended for production)
CORS_ALLOW_ALL=false

# Comma-separated list of allowed origins (ignored if CORS_ALLOW_ALL=true)
# Example: https://app.example.com,https://admin.example.com
CORS_ALLOWED_ORIGINS=http://localhost:8081,exp://localhost,exp://127.0.0.1

